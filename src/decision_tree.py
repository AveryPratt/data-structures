"""Implementation of decision tree for predicting properites of objects based on location on a graph."""


class DecisionTree(object):
    """A class for creating a layout for predicting groups of objects on a graph.

    fit(self, data, groups):
    Maps the graph into sections where points are likely to be one group or the other.

    predict(self, data):
    assigns points of data to different groups based on where they are on the graph.
    """

    def __init__(self, max_depth=None, min_leaf_size=None):
        """Creates a new instance of a decision tree which can then take data."""
        pass

    def fit(self, data, groups):
        pass

    def predict(self):
        pass
